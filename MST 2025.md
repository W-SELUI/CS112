---

---

Section A : Multiple Choice

---

1. Consider the following C++ code
```c++
int f(int x){
	x = 10;
}
```

What is the outcome of the statement cout<<f(5) ; used in main( )?
a. 5
b. f(5)
c. 10
d. Error, 0 or a random value

2. What is the outcome of the following C++ program
```c++
int arr[] = {3, 4, 5};
int p = arr;
cout << *p;
```
a. Some address
b. 3
c. {3, 4, 5}
d. an error

3. Why using classes for large programs are recommended? 
a. program becomes fast
b. Its easy to use the code
c. Easy to organize the code
d. Have less bugs in the code

4. Which of the following is false about inheritance in C++?
a. Inheritance helps is reusing the code from a base class
b. private members are not inherited
c. derived class can inherit multiple base classes
d. derived class can access the protected base class member

5. If an array has n elements, what is the index of the last element
a. n
b. n+1
c. n-1
d. 0

---

Section B : Short Answers

1. What is the 3 main pillars of object-oriented programming?

2. What kind of relationship should exist between the Student and the Course class? State any special assumptions you may have

3. What is the main role of a constructor in C++ ?

4. Why is method overriding useful?

---

Section C : Program Writing

1. What is wrong with the following code? How would you correct it without entirely removing the chosen code snippet? Mention line numbers in your answer? 

```c++
class Point{                 // line 6
	private:
		Point(){;}
    protected:
	    int x;
	    int y;
};


class Circle: public Point{
	private:
		Circle();
	public:
		int radius;
		
};

Circle::Circle()
:Point(){
	;
}                         // line 26
```

What is wrong? 

Line number(s) : 

How to correct?

Line Number(s):


2. Consider the following  code and answer the given questions 

```c++
class Shape{
	public:
		virtual float area() = 0;
};

class Circle: public Shape{
	private:
		Circle(){;}
	public:
		Circle(float radius){
			this->radius = radius;
			}
			float radius;
			virtual float area()//complete this code
			
};

int main()
{
	Shape *s;
	//To Do
	
```

a. Why is there NO implementation for the area( ) method in the Shape class?

b. Complete the implementation for the area( ) method in the Circle class?

c. Using the shape pointer declared on line 25, show how you can call the area( ) method of the Circle class of radius 5 [Hint: you may need more than one line of code to accomplish this]


3. Consider the following code

```c++
#include <iostream>
using namespace std;

struct Student{
	int id;
	string name;
};

int main(){
	Student s[5] = {
			{101, "Ali"},
			{102, "Ravi"},
			{103, "Lina"},
			{104, "Maya"},
			{105, "John"}
		};
		
		//To Do
};
```

Task : Complete the main( ) to print the id and name all 5 students using a loop




